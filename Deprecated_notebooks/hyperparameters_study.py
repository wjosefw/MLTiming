import numpy as np


# Semilla de la red
seed = np.array([])  
FWHM_results = np.array([])
bias_results = np.array([])

#Semilla del dataset
seed = np.array([])  
FWHM_results = np.array([])
bias_results = np.array([])

#max delay
max_delays = np.array([])
FWHM_results = np.array([])
bias_results = np.array([])